# Database Configuration
# For local development, you can use a local Postgres instance or leave empty to skip database features
# Example: postgresql://user:password@localhost:5432/crib
DATABASE_URL=

# For Railway deployment, Railway will automatically inject this variable
# Format: postgresql://user:password@host:port/database

# Google OAuth Client ID (strict audience check)
GOOGLE_CLIENT_ID=
